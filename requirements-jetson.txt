# Face Verification System - Jetson Nano Requirements
# Optimized for NVIDIA Jetson Nano with L4T and TensorFlow pre-installed

# Note: TensorFlow comes pre-installed in the base image (l4t-tensorflow)
# Do not install tensorflow via pip on Jetson Nano

# Core ML / numeric libs (compatible with ARM64)
numpy>=1.19.0,<1.24.0
scipy>=1.5.0,<1.10.0

# Computer vision / image processing
# OpenCV is pre-installed via apt in Dockerfile
opencv-python>=4.5.0

# Image processing
Pillow>=8.0.0,<10.0.0

# Plotting / utilities
matplotlib>=3.3.0,<3.7.0
scikit-learn>=0.24.0,<1.2.0

# HDF5 support for model loading
h5py>=2.10.0,<3.8.0

# Optional: DeepFace for gender/face attribute analysis
# Note: May have performance issues on Jetson Nano
# deepface>=0.0.75

# Additional utilities
tqdm>=4.50.0
pyyaml>=5.4.0

# For future REST API support (optional)
# flask>=2.0.0
# flask-cors>=3.0.10
